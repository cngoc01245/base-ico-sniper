{
  "name": "base-ico-sniper",
  "version": "1.0.0",
  "description": "High-performance EVM sniper proxy for Base network",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.ts --network base_mainnet",
    "snipe": "hardhat run scripts/snipe.ts --network base_mainnet"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.22.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0",
    "dotenv": "^16.4.5"
  }
}
